# 端口号配置
server:
  port: 8887


spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost/community?serverTimezone=UTC
    driver-class-name: com.mysql.jdbc.Driver
  thymeleaf:
    cache: false